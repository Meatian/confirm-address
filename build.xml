<?xml version="1.0" encoding="utf-8"?>
<project name="build xpi" default="build" basedir=".">

	<property file="build.properties"/>
	
	<target name="build">
		<echo>${dist}</echo>
		<zip destfile="${dist}"
			basedir="${base}"/>
	</target>
	
</project>